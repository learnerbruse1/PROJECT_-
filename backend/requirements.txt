fastapi==0.115.0
uvicorn[standard]==0.30.0
pydantic==2.9.0
asyncpg==0.29.0
# Spatial computation library required by the file-based backend (DATA_BACKEND=geojson, default).
# Not required for PostGIS backend, but installing it allows switching between both modes freely.
shapely>=2.0
